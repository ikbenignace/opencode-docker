services:
  opencode:
    build: .
    image: ghcr.io/ikbenignace/opencode-docker:latest
    container_name: opencode-web
    ports:
      - "4096:4096"
    volumes:
      # Persistent storage for opencode data
      - ./data:/data
    environment:
      - OPENCODE_SERVER_PORT=4096
      - OPENCODE_SERVER_HOSTNAME=0.0.0.0
    restart: unless-stopped
